AOS — MILESTONES (quick-glance progress tracker)
Last updated: 2026-08-11
Current commit / checkpoint: Phase 5 (Loan outcome tracking) complete — see git log for the commit hash
Full detail: see "AOS Production Readiness Master Roadmap.txt"

============================================================
ORIGINAL SEQUENCE (early planning list — history, preserved as-is)
============================================================

The sequence I'd follow
✅ Architecture
✅ Entity Management
✅ Financial-Year Documents
✅ Automatic Storage
✅ Master Data Engine
✅ Master Data Refinement
✅ Lending Product Catalogue
✅ Lending Product Refinement
✅ Bank & NBFC Catalogue
✅ ✅ Document Requirement Engine
✅ Bank Catalogue & Submission Workflow
Workflow Polish
OCR & AI
Email / WhatsApp Integration
Management Dashboards

============================================================
PRODUCTION READINESS ROADMAP (current tracking structure)
============================================================

Stage 1 — Persistence ✓
Stage 2 — Server/API + Customer/Case Vertical Slice ✓
Stage 3A–3D — PostgreSQL Migration / Operational Vertical Slices ✓

Stage 4 — Production Readiness
  Item 1 — Backup & Disaster Recovery ✓
  Item 2 — Deployment Topology ✓
  Item 3 — Authentication & Security ✓
  Item 4 — Master Data & Configuration ✓
  Follow-up — PostgreSQL enum-array parsing fix ✓

  Phase 1 — Column Masking / ADR-026 ✓
  Phase 2 — Admin-screen honesty ✓
  Phase 3 — Concurrency & audit hardening ✓
  Phase 4 — Case completeness ✓
  Phase 5 — Loan outcome tracking ✓

Office Server Production Cutover Gate ○ [ ]
  (separate from development phases — backup, migrations, aos_app
  production credentials, .env, bootstrap-production, service/firewall
  setup, second-PC verification, reboot recovery, Gmail credentials,
  end-to-end verification, backup/restore on real hardware — see master
  roadmap for the full 21-step checklist)

INITIAL PRODUCTION ○ [ ]
  (final milestone — initial intake/document/submission workflow live
  on the office server)

============================================================
STATE AS OF THIS CHECKPOINT
============================================================

HEAD: see git log (Phase 5 commit, this checkpoint)
origin/main: pushed by this change
Working tree: clean except pre-existing untracked scratch files

Current active work: Phase 5 complete. No development phase is currently
queued — next is the Office Server Production Cutover Gate, whenever the
business is ready for it. Inbound bank-email ingestion remains explicitly
deferred.

Phase 5 in one line: bank submissions now carry their real lifecycle
end to end — under process, bank queries raised and answered, eligibility,
sanction (backed by an offer, BR-023), rejection (backed by a reason,
BR-024), withdrawal and disbursement (one per case, BR-022) — all through
the `submission`/`offer`/`submission_query`/`rejection_reason` schema that
already existed since Stage 3D (zero migrations), the existing
`submission.update_status`/`offer.read`/`offer.record`/`offer.accept`
permissions (no catalog change), and the existing `deriveSystemStagePath`
(`Backend/case-stage.ts` is the new, single place that walks it, replacing
three separate hard-coded-`false` snapshot builders in `cases.ts`,
`documents.ts` and `submissions.ts`). Accepting an offer withdraws every
other live submission on the case in one transaction (BR-025), each
withdrawal individually audited. BanksTab.tsx gained the full operational
UI; CaseDetail's Move Stage UI needed no change — sanctioned/disbursed were
already excluded as system-actor transitions. Full integration suite green
(180 tests, including the two `security*.test.ts` files the Phase 4 note
flagged as a worktree gap — clean in this environment). 8 new integration
tests in `Backend/submission-outcomes.test.ts`, 8 new domain tests in
`src/domain/submissions/status.test.ts`. See the master roadmap's Phase 5
section for full detail.
